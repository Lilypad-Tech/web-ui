const Avatar = ({}) => {
	return (
		// TODO this is the avataratom
		<div className={avatarSizes}>
			<img
				src={src}
				alt="Avatar"
				className=" 
		rounded-full border-uui-avatar-contrast-border object-cover bg-center bg-contain bg-no-repeat"
			/>
		</div>
	);
};
